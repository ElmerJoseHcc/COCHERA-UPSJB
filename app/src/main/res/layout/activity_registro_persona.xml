<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Campo Nombre -->
        <EditText
            android:id="@+id/edtNombre"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Nombre completo"
            android:inputType="textPersonName" />

        <!-- Campo Apellido -->
        <EditText
            android:id="@+id/edtApellido"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Apellido Completo"
            android:inputType="textPersonName" />

        <!-- Texto Seleccione Carrera -->
        <TextView
            android:id="@+id/textSeleccioneCarrera"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Seleccione su carrera:"
            android:textColor="@android:color/holo_red_dark"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Spinner -->
        <Spinner
            android:id="@+id/spinnerCarrera"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="15dp"
            tools:ignore="TouchTargetSizeCheck" />

        <!-- Ciclo -->
        <EditText
            android:id="@+id/edtCiclo"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Ciclo"
            android:inputType="number" />

        <!-- DNI -->
        <EditText
            android:id="@+id/edtDNI"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="DNI"
            android:inputType="number" />

        <!-- Teléfono -->
        <EditText
            android:id="@+id/edtTelefono"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="Teléfono"
            android:inputType="phone" />

        <!-- Botón Guardar -->
        <Button
            android:id="@+id/btnGuardarPersona"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Guardar Persona" />
    </LinearLayout>
</ScrollView>
